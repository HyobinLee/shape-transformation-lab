# 한글 폰트 (4.5MB). 어떤 코드도 이 .ttf 를 읽지 않는다 —
# 섹션은 전부 Plotly 로 그리고, Plotly 는 브라우저에 설치된 폰트를 쓴다.
fonts/

# Python
__pycache__/
*.py[cod]

# Virtual environments (각 머신에서 직접 생성)
.venv/
venv/
env/

# Streamlit
.streamlit/secrets.toml
